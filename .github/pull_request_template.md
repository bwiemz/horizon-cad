## Why

<!-- What was wrong or missing, in the user's terms. -->

## What

<!-- What changed, and how it fixes that. -->

## Checks

- [ ] Tests cover the change (`tests/<module>/`; window behaviour in `tests/ui/`).
- [ ] `ctest` passes locally.
- [ ] `clang-format` is clean (CI uses clang-format 15; see CONTRIBUTING).
- [ ] The CHANGELOG's Unreleased section says what changed, for a user.
