#pragma once

// Generated by CMake from cmake/Version.h.in. The version is set once, in
// project() in the top-level CMakeLists.txt; everything else — the About box,
// --version, the log, the installer — reads it from here or from CMake.

namespace hz::version {

inline constexpr int kMajor = @PROJECT_VERSION_MAJOR@;
inline constexpr int kMinor = @PROJECT_VERSION_MINOR@;
inline constexpr int kPatch = @PROJECT_VERSION_PATCH@;
inline constexpr const char* kString = "@PROJECT_VERSION@";

}  // namespace hz::version
